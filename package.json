{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "Quiz Application",
  "scripts": {
    "start": "cd server && node server.js",
    "heroku-postbuild": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

