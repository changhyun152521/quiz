# ============================================================
# mathchang-quiz 환경변수 설정
# ============================================================
#
# 인증 통합 설명:
# - mathchang-quiz는 mathchang의 인증 API를 프록시하여 사용합니다
# - 로그인 요청은 MATHCHANG_API_URL로 전달됩니다
# - JWT_SECRET은 mathchang과 동일해야 토큰 검증이 가능합니다
# - MongoDB는 각각 분리되어 있습니다
#
# ============================================================

# Server Port
PORT=5000

# MongoDB Connection String (mathchang-quiz 전용 DB)
MONGODB_ATLAS_URL=your_mongodb_atlas_connection_string_here

# 환경 설정
NODE_ENV=development

# ⚠️ mathchang API URL (인증 프록시용)
# 로그인/아이디찾기 요청이 이 URL로 전달됩니다
MATHCHANG_API_URL=https://api.mathchang.com

# ⚠️ JWT Secret Key
# 중요: mathchang과 동일한 JWT_SECRET을 사용해야 토큰 검증이 가능합니다
# mathchang의 .env 파일에서 JWT_SECRET 값을 복사하세요
JWT_SECRET=your-super-secret-key-change-in-production

# Cloudinary 설정 (이미지 업로드용)
CLOUDINARY_CLOUD_NAME=your-cloud-name-here
CLOUDINARY_API_KEY=your-api-key-here
CLOUDINARY_API_SECRET=your-api-secret-here
CLOUDINARY_UPLOAD_PRESET=unsigned

# 프론트엔드 URL (CORS 설정용)
FRONTEND_URL=http://localhost:5173
